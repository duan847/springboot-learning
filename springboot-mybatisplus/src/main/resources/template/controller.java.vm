package $!{package.Controller};
import com.baomidou.mybatisplus.mapper.EntityWrapper;
import com.baomidou.mybatisplus.plugins.Page;
import com.carding.bow.common.util.Query;
import com.carding.bow.common.util.R;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import io.swagger.annotations.Api;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;
import $!{package.Entity}.$!{entity};
import $!{package.Service}.$!{entity}Service;
#if($!{superControllerClassPackage})
import $!{superControllerClassPackage};
#end

import java.util.Date;
import java.util.Map;

/**
 * <p>
 * $!{table.comment} 前端控制器
 * </p>
 *
 * @author $!{author}
 * @since $!{date}
 */
@Api("$!{table.comment}")
@RestController
@RequestMapping("/$!{table.entityPath.toLowerCase()}")
public class $!{table.controllerName} extends $!{superControllerClass} {

    @Autowired
    private $!{entity}Service $!{table.entityPath}Service;

    /**
     * 新增$!{table.comment}
     * @param  $!{table.entityPath}  实体
     * @return success/false
     */
    @PostMapping
    @ApiOperation("020201-01 新增$!{table.comment}")
    public R<Boolean> add(@RequestBody $!{entity} $!{table.entityPath}) {
        return new R<>($!{table.entityPath}Service.insert($!{table.entityPath}));
    }

    /**
     * 根据id查看$!{table.comment}
     *
     * @param id ID
     * @return $!{entity}
     */
    @GetMapping("/{id}")
    @ApiOperation("020201-02 根据id查看$!{table.comment}")
    public R<$!{entity}> get(@PathVariable String id) {
        return new R<>($!{table.entityPath}Service.selectById(id));
    }

    /**
     * 分页查询$!{table.comment}
     *
     * @param params 分页对象
     * @return 分页对象
     */
    @GetMapping("/page")
    @ApiOperation("020201-03 分页查询$!{table.comment}")
    public Page page(@RequestParam Map<String, Object> params) {
        return $!{table.entityPath}Service.selectPage(new Query<>(params), new EntityWrapper<>());
    }

    /**
     * 根据id删除$!{table.comment}
     * @param id ID
     * @return success/false
     */
    @DeleteMapping("/{id}")
    @ApiOperation("020201-04 根据id删除$!{table.comment}")
    public R<Boolean> delete(@PathVariable String id) {
        return new R<>($!{table.entityPath}Service.deleteById(id));
    }

    /**
     * 根据id修改$!{table.comment}
     * @param  $!{table.entityPath}  实体
     * @return success/false
     */
    @PutMapping
    @ApiOperation("020201-05 根据id修改$!{table.comment}")
    public R<Boolean> edit(@RequestBody $!{entity} $!{table.entityPath}) {
        return new R<>($!{table.entityPath}Service.updateById($!{table.entityPath}));
    }
}
